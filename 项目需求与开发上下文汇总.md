# 工程管理智能体项目需求与开发上下文汇总

## 1. 文档目的

本文汇总当前对话中已经确认的需求、架构决策、开发约束、环境信息和实施进度，作为后续继续开发 `smart-agent` 的统一上下文。本文不记录数据库密码、API Key、本地签名密钥等敏感值。

更新时间：2026-09-01

## 2. 项目定位

- 系统名称：工程管理智能体（`smart-agent`）。
- 当前只服务现有工程管理系统，不建设通用的多业务平台智能体。
- 后续是否抽象成可搭载任意业务平台的通用产品，留到工程系统版本稳定后再讨论。
- 用户侧采用统一入口，只展示一个“工程管理智能助手”。
- 系统内部按项目、合同、进度、预算、财务、招投标、档案、报告等领域动态加载能力，不向用户暴露多个智能体。
- 现有 `smart-boot` 继续作为业务数据、业务规则和权限判断的最终权威。

## 3. 建设目标

一期目标是建设安全、可追溯的只读智能问答能力：

1. 查询项目、合同、进度、预算、财务等实时业务数据。
2. 检索制度、规范、合同、项目文档和系统附件。
3. 将实时业务数据与知识库依据结合，生成带引用的回答。
4. 支持跨模块综合分析、风险提示和报告草稿。
5. 记录会话、模型调用、知识检索、工具调用、异常和审计信息。
6. 严格继承当前登录用户的权限，不允许通过请求参数伪造租户或项目范围。

当前不做：

- 通用 Connector SDK 和第三方平台生态；
- 跨平台身份体系；
- 直接访问生产业务表的自由 SQL 智能体；
- 无需确认的高风险自主写操作；
- 面向任意业务平台的通用智能体中台。

## 4. 已确认技术架构

### 4.1 smart-agent

- Java 21
- Spring Boot 3
- LangChain4j
- Maven 单工程，按 package 分层
- MySQL：保存智能体配置、会话、运行记录、审计和知识元数据
- Redis：缓存、限流、短期状态和异步任务状态
- Qdrant：知识向量和工具语义索引
- MinIO / OSS：复用现有文件存储能力，保存原始文件和解析结果
- 模型网关：屏蔽具体模型供应商，支持云端、本地或混合接入

### 4.2 现有工程系统

- `smart-web`：提供统一对话中心和业务页面侧边助手。
- `smart-boot`：现有 Java 8 / Spring Boot 2.7 后端，负责业务事实、业务规则和最终权限校验。
- `smart-agent` 不直接修改现有业务数据库。
- 业务数据通过 `smart-boot` 的内部 AI 工具适配接口访问。

### 4.3 核心调用关系

```text
smart-web
  -> HTTPS + SSE
smart-agent
  -> 身份上下文、编排、工具、RAG、模型网关、审计
  -> smart-boot 内部 AI 适配接口
  -> MySQL / Redis / Qdrant / MinIO或OSS
```

## 5. 权限与安全要求

- 智能体必须继承当前用户的租户、身份、角色、部门、公司、项目范围、菜单按钮权限和数据范围权限。
- 前端传递的页面、项目和业务对象信息仅作为上下文提示，不能作为可信权限依据。
- `smart-agent` 首先筛选用户可用的工具和知识空间。
- `smart-boot` 在工具真正执行前再次校验用户身份、业务权限、项目权限和数据范围。
- 禁止请求通过 `tenantId`、`userId`、权限 Header 或 JSON 字段覆盖可信身份上下文。
- 一期业务工具以 L0/L1 只读工具为主。
- 后续写操作按照风险等级管理，默认需要预览、用户确认、权限复核和审计。
- 不保存或展示模型内部思维链。
- 不允许任意 SQL，不允许智能体直接更新生产业务表。
- 知识库内容视为不可信输入，需要防止提示词注入。

## 6. 知识库设计

- MySQL 保存知识空间、文档、版本、ACL、切片和任务元数据，并作为权威数据源。
- Qdrant 仅作为可重建的向量检索索引。
- 开发环境计划使用局域网 Docker 中的 Qdrant 单节点。
- 建议开发 Collection：`agent_knowledge_dev`、`agent_tool_index_dev`。
- 检索必须强制过滤租户、知识空间、项目范围和发布状态。
- 回答需要提供文件、版本、页码、章节、原文片段等引用信息。
- 一期计划支持 PDF、Word、Excel、Markdown、TXT 和系统已有附件。
- OCR、PPT 深度解析、混合检索和重排序可后续增强。

## 7. 模型接入设计

- 所有模型调用统一通过 `ModelGateway`，业务代码不绑定具体模型供应商。
- 网关后续支持对话模型、推理模型、摘要模型、Embedding 模型和重排序模型。
- 当前尚未确定或购买真实大模型接口。
- 当前开发使用确定性的本地模拟模式，保证测试不依赖外部模型。
- 后续真实模型需要重新确定供应商、Base URL、模型名称、API Key、超时、降级和费用策略。

## 8. 当前开发环境

### 8.1 代码目录和分支

- Git 仓库主目录：`F:\project\gongcheng\agent`
- 主目录当前分支：`main`
- 一期功能分支：`feature/phase1-agent-backend`
- 实际开发 worktree：`F:\project\gongcheng\agent\.worktrees\phase1-agent-backend`
- 后续编码、测试和启动应在该 worktree 中执行。
- 功能全部完成并验证后，再决定是否合并回 `main`。

### 8.2 本地配置文件

- 实际配置文件：`F:\project\gongcheng\agent\.worktrees\phase1-agent-backend\.env`
- 配置模板：`F:\project\gongcheng\agent\.worktrees\phase1-agent-backend\.env.example`
- `.env` 已被 Git 忽略，不应提交。
- 本地签名密钥已随机生成并仅写入 `.env`。

### 8.3 已确认连接信息

| 组件 | 当前配置 | 状态 |
|---|---|---|
| smart-agent | `localhost:8080` | 默认端口已确认 |
| MySQL | `127.0.0.1:3306` | 已安装，数据库已创建 |
| 数据库 | `smart_agent` | 已创建 |
| MySQL 用户 | `root` | 密码仅保存在本地 `.env` |
| Redis | `127.0.0.1:6379` | 本地运行，无密码 |
| 局域网 Docker 主机 | `192.168.0.179` | 后续用于开发基础设施 |
| Qdrant | 预留 `192.168.0.179:6334` | 尚未安装，暂不联调 |
| 大模型接口 | 无 | 使用本地模拟模式 |

### 8.4 Docker约束

- 不在当前开发机启动本地 Docker。
- Docker 服务安装在局域网设备 `192.168.0.179`。
- 不开放未认证的 Docker TCP API。
- 如需从开发机部署，使用 SSH Docker Context 或安全 TLS 连接。
- 所有 MySQL、Redis、Qdrant、模型和对象存储地址必须通过配置注入，不能写死。
- 需要 Docker 的集成测试只有在局域网 Docker 坐标可用时才运行。
- Qdrant 尚未安装，因此当前不能声称 Qdrant 集成测试已通过。

## 9. 已完成开发内容

当前功能分支最新已知提交：`197393b`。

### Task 1：服务基础工程

- 完成 Java 21、Spring Boot、LangChain4j、MySQL、Redis、Qdrant 等依赖和基础配置。
- 完成远程局域网 Docker 配置方式。
- 默认安全绑定回环地址，支持外部配置。
- 删除不安全的未认证 Docker TCP 示例。
- 配置 `.env` 本地导入流程。

相关提交：

- `b25910d build: bootstrap smart agent service`
- `6cc92cb fix: support secure LAN Docker development`

### Task 2：会话和运行持久化

- 完成会话和 Agent Run 领域模型、服务、仓储和数据库迁移。
- 查询按租户和用户隔离。
- 增加组合所有权约束、输入校验和配置开关。
- 提供内存单元测试仓储和可选 MySQL/Testcontainers 集成测试。

相关提交：

- `a99bbf6 feat: persist conversations and agent runs`
- `a428069 fix: enforce scoped conversation persistence`

### Task 3：可信工程用户上下文

- 完成不可变 `AgentUserContext`。
- 完成请求与线程上下文传递、异常和结束后的强制清理。
- 完成本地 HMAC 紧凑令牌验证和过期检查。
- 完成 `/agent/**` 身份过滤。
- 完成稳定的 HTTP 401 / `AGENT_UNAUTHORIZED` 错误。
- 完成 `X-Trace-Id` 校验、生成和响应传递。
- 防止请求参数、伪造 Header 和验证器内部异常泄露。

相关提交：

- `197393b feat: verify engineering user context`

验证记录：

- Task 3 安全测试 20/20 通过。
- 当前非 Docker 测试 33/33 通过。
- Task 3 独立审查无遗留问题。
- MySQL、Qdrant 的 Docker 集成测试仍按环境约束推迟。

## 10. 后续实施任务

### 10.1 总任务计划与当前状态

任务按依赖关系顺序执行。当前没有确定具体日历日期，因此以任务完成和审查通过作为进入下一任务的门槛。

| 顺序 | 任务 | 状态 | 主要依赖 | 核心交付物 | 完成门槛 |
|---|---|---|---|---|---|
| 1 | 服务基础工程 | 已完成 | 无 | Java 21工程、配置、Compose开发模板 | 测试、打包和安全配置审查通过 |
| 2 | 会话与运行持久化 | 已完成 | Task 1 | 会话、运行状态、仓储、Flyway迁移 | 租户隔离测试和独立审查通过 |
| 3 | 可信用户上下文 | 已完成 | Task 1 | 身份令牌、过滤器、TraceId、安全错误 | 安全测试20/20和独立审查通过 |
| 4 | 权限感知业务工具 | 下一任务 | Task 2、3 | ToolExecutor、项目概况工具 | 权限、范围、输入、超时、结果限制测试通过 |
| 5 | 模型网关 | 待开始 | Task 1、4 | 本地模拟与OpenAI兼容模型适配 | 网关契约和异常测试通过 |
| 6 | 知识元数据与索引 | 环境待准备 | Task 1、3 | MySQL知识表、切片、Embedding、Qdrant索引 | 单元测试及Qdrant集成测试通过 |
| 7 | 知识检索与引用 | 待开始 | Task 6 | 权限过滤检索、文档引用 | 租户/项目隔离及引用测试通过 |
| 8 | 对话编排与SSE | 待开始 | Task 2至7 | `/agent/chat/stream`完整垂直链路 | SSE端到端测试和审计持久化通过 |
| 9 | 架构与一期验收 | 待开始 | Task 1至8 | 架构规则、验收测试、运维手册 | 一期自动化验收全部通过 |

### 10.2 任务执行规则

每个任务统一采用以下流程：

```text
确认任务边界
-> 先写失败测试并确认失败原因
-> 实现最小可用代码
-> 运行专项测试
-> 运行全部非Docker测试
-> 自检代码差异和敏感信息
-> 提交独立Git commit
-> 独立代码审查
-> 修复重要问题并复审
-> 更新进度记录
-> 进入下一任务
```

- 一个任务未通过专项测试、全量测试和独立审查，不进入下一任务。
- 发现缺陷时先补充能够复现问题的测试，再修复实现。
- Docker环境未就绪不能阻止其他独立单元任务，但相关集成任务不能标记完成。
- 所有提交保留小范围、可审查、可回退的边界。

### 10.3 阶段和里程碑安排

| 里程碑 | 包含任务 | 结果 |
|---|---|---|
| M1 基础底座 | Task 1至3 | 服务、持久化和可信身份上下文可用，已达到 |
| M2 业务问答内核 | Task 4至5 | 能够安全选择业务工具并通过模型网关编排 |
| M3 知识库内核 | Task 6至7 | 能够摄取、过滤检索并返回可靠引用 |
| M4 一期垂直链路 | Task 8 | 对话、工具、知识、模型、SSE和审计串联 |
| M5 一期交付 | Task 9 | 架构规则、运维说明和自动化验收完整 |

当前处于 **M2 的 Task 4 开始前**。Task 6 开始前必须先完成局域网 Qdrant 安装和连通性确认。

### 10.4 Task 4：首个权限感知业务工具

- 实现统一 `AgentTool`、`ToolContext`、`ToolExecutor` 和风险等级。
- 实现 `project.getOverview` 只读工具。
- 检查工具权限、项目范围、未知输入字段、超时和结果大小。
- 通过 `smart-boot` 的 `ProjectBusinessClient` 获取真实业务数据；本地测试使用确定性适配器。

计划测试：

- 已授权项目可以执行查询。
- 缺少 `project:read` 权限时拒绝。
- 项目不在用户可信范围时拒绝且不调用业务客户端。
- 未知工具、未知输入字段、超时和超过64KB结果被稳定处理。

建议提交信息：`feat: add permission aware project tool`

### 10.5 Task 5：模型网关

- 实现模型中立的流式事件接口。
- 实现本地确定性网关和 OpenAI-compatible 适配器。
- 处理工具请求、文本流、超时、错误映射和 Token 元数据。

计划测试：

- 本地模拟网关可以确定性地产生项目工具请求和最终文本。
- 文本增量、工具参数、完成和失败事件契约稳定。
- 模型超时、供应商错误和畸形工具参数不会泄露内部信息。
- 真实供应商未配置时，非相关测试不依赖外部网络。

建议提交信息：`feat: add pluggable model gateway`

### 10.6 Task 6：知识元数据和 Qdrant 索引

- 创建知识库相关 MySQL 表。
- 实现文本切片、校验、哈希、Embedding 和向量写入。
- 实现 Qdrant Collection、Payload 索引和权限过滤。
- 该任务需要先在 `192.168.0.179` 安装 Qdrant。

环境前置：

- Qdrant安装在 `192.168.0.179`，默认计划使用gRPC端口 `6334`。
- 确认数据卷、Collection、API Key和防火墙来源限制。
- 应用开发机必须能够访问Qdrant端口。

计划测试：

- 文本为空或超过1MB时拒绝。
- 切片长度、重叠、校验和及重复索引行为确定。
- 向量检索不会返回其他租户、草稿或无权项目的数据。
- MySQL元数据失败时不会留下不一致的已发布索引。

建议提交信息：`feat: index knowledge in qdrant`

### 10.7 Task 7：权限过滤的知识检索和引用

- 使用可信用户上下文生成检索过滤条件。
- 从 MySQL 复核权威文档状态，丢弃陈旧或已删除向量结果。
- 返回不可变且可定位的文档引用。

计划测试：

- 空问题、无权项目、空知识空间和非法TopK被拒绝。
- 租户、知识空间、项目和发布状态过滤生效。
- 删除、过期版本和陈旧向量结果不会返回。
- 引用包含稳定的文档、版本、页码、章节和原文定位。

建议提交信息：`feat: retrieve permission filtered knowledge`

### 10.8 Task 8：统一对话编排和 SSE 接口

- 实现 `POST /agent/chat/stream`。
- 串联会话、运行状态、模型、工具和知识检索。
- 流式返回状态、工具调用、引用、文本增量、结束和错误事件。
- 设置模型轮数、工具次数、引用数量、总时长和结果大小限制。

编排顺序：

1. 创建运行记录并保存用户消息。
2. 进入路由和规划状态，只加载用户允许的能力和工具。
3. 按问题需要执行知识检索。
4. 调用模型；工具请求必须经过 `ToolExecutor`。
5. 先输出引用，再输出回答文本增量。
6. 保存最终回答、引用、工具摘要、Token、状态和错误码。

运行限制：最多6轮模型调用、5次工具调用、20条引用、90秒总时长、单个工具结果64KB。

计划测试：普通回答、项目工具、知识引用、缺失权限、模型超时、畸形参数、客户端取消和TraceId传播。

建议提交信息：`feat: stream audited agent conversations`

### 10.9 Task 9：架构规则、运维文档和验收

- 增加 ArchUnit 架构约束。
- 增加 MySQL、Qdrant、SSE 的端到端验收测试。
- 编写本地开发和 Qdrant 重建手册。
- 完成一期全量验证。

最终验证包括：

- 全部单元测试和安全测试。
- MySQL迁移及持久化集成测试。
- Qdrant隔离、索引和重建测试。
- SSE完整对话链路测试。
- 架构依赖规则。
- 配置文件安全检查和代码格式检查。

建议提交信息：`test: verify phase one agent vertical slice`

### 10.10 后续二期和三期方向

一期完成后再单独制定详细计划：

- 二期：财务、招投标、档案等工具，指标中心，跨领域分析，OCR/PPT，混合检索、重排序、报表和风险清单。
- 三期：受控写操作、人工确认恢复、更多治理评测、用量成本、多模型路由和生产高可用。
- 通用多平台智能体不在当前任务计划内。

## 11. 一期验收标准

一期只有同时满足以下条件才算完成：

- Java 21 应用可连接已迁移的 MySQL 并成功启动。
- 缺失、过期或伪造的工程用户上下文被拒绝。
- 用户不能通过请求 JSON 伪造租户或项目权限。
- 至少一个项目只读工具只能在授权项目范围内执行。
- 文档元数据写入 MySQL，切片向量写入 Qdrant。
- Qdrant 检索严格执行租户、知识空间、项目和发布状态过滤。
- 一个问题可以通过 SSE 同时返回业务事实和知识引用。
- 会话、运行步骤、工具摘要、引用、Token 和终态可以审计。
- 模型或工具异常返回稳定错误，不泄露密钥和敏感内容。
- 不存在生产写工具和任意 SQL 能力。
- 全量测试、架构测试和代码格式检查通过。

## 12. 尚未确定或待补充信息

- Qdrant 的实际安装时间、镜像版本、数据卷、备份方式和 API Key。
- 真实大模型供应商、模型名称、接口地址、费用和降级策略。
- Embedding 与重排序模型选型。
- MinIO / OSS 的具体接入地址、Bucket、凭证和现有文件接口。
- `smart-boot` 内部 AI 适配接口的认证方式和最终字段协议。
- 现有角色、部门、公司、项目、菜单按钮和数据范围的具体映射规则。
- 一期优先接入的业务工具清单和业务指标口径。
- 知识空间管理员、文档发布流程和权限变更同步规则。
- 生产部署资源、域名、证书、监控、日志和备份策略。

## 13. 继续开发时的操作原则

1. 在 `F:\project\gongcheng\agent\.worktrees\phase1-agent-backend` 中继续开发。
2. 不重做已经完成并审查通过的 Task 1 至 Task 3。
3. 从 Task 4 开始，继续使用测试先行、单任务提交和独立代码审查。
4. 不启动本地 Docker，不绕过局域网 Docker 的安全限制。
5. 不把 `.env`、密码、签名密钥或 API Key 提交到 Git。
6. Docker 依赖不可用时，只运行非 Docker 测试，并明确记录推迟项。
7. 所有权限判断必须来自可信上下文，并由 `smart-boot` 最终复核。

## 14. 现有详细文档

- 系统总体设计：`docs/superpowers/specs/2026-08-31-smart-agent-system-design.md`
- 一期实施计划：`docs/superpowers/plans/2026-08-31-phase-1-agent-backend-vertical-slice.md`
- SDD 进度：`.superpowers/sdd/2026-08-31-phase-1-agent-backend-vertical-slice/progress.md`
- Task 3 报告：`.superpowers/sdd/2026-08-31-phase-1-agent-backend-vertical-slice/task-3-report.md`

上述详细文档当前位于功能 worktree 中。

## 15. 2026-09-08 当前开发进度（新对话续接入口）

本节为当前实际开发状态，后续新对话应优先阅读本节，不要从 Task 4 重新开始。

### 15.1 当前总体状态

- 后端 `smart-agent` 已完成会话、消息、运行记录、SSE 对话、外部模型调用、附件处理和知识库基础能力的演示链路。
- 前端 `smart-web` 已完成智能助手页面、会话列表、会话切换、历史消息恢复、标题修改、删除聊天、复制粘贴上传附件和聊天输入交互。
- 当前主要工作已从基础架构转入“智能助手演示体验修复和附件/会话细节完善”。
- 用户自行编译和启动，助手只修改代码并汇报修改文件，不代替用户编译。
- 当前未完成的重点是：业务工具接入、业务数据查询能力、附件随消息传递到后端后的完整联调，以及前端细节问题验证。

### 15.2 已完成的前端功能

涉及工程：`F:\project\gongcheng\smart-web`

- 智能助手页面和会话列表。
- 新建对话；首次发送消息时没有会话则自动创建。
- 首条消息前 20 个字符作为默认标题。
- 左侧会话标题超长省略。
- 页面刷新后默认加载最后一个会话及其历史消息。
- 点击左侧会话后重新加载对应历史消息，避免右侧仍显示原会话内容。
- 会话标题修改浮动菜单。
- 会话删除浮动菜单和确认弹窗。
- 删除成功后才关闭确认弹窗；删除失败时保留弹窗。
- 修复删除接口返回 204 时前端响应拦截器报 `undefined.indexOf` 的问题。
- 等待模型响应显示“正在思考”。
- 停止按钮放在发送按钮位置。
- Markdown 基础标题、粗体、行内代码、代码块和换行显示。
- 对话区域内部滚动，避免页面整体滚动。
- 复制粘贴上传文件/图片，附件可和文字组合发送，也可单独发送。
- 附件预览放在输入框上方，不再显示“已上传”文字；右上角使用居中的 `x` 删除按钮。
- 上传进度使用环形进度显示；上传成功后移除进度/成功勾选状态。
- 已移除显式“上传附件”按钮，当前以复制粘贴上传为主。

### 15.3 当前前端关键文件

- `smart-web/src/views/ai/assistant/index.vue`
  - 智能助手页面、会话列表、切换、删除、改标题、发送、附件组合发送。
- `smart-web/src/views/ai/assistant/composables/use-conversations.js`
  - 会话列表、创建、选择、历史、改标题、删除。
- `smart-web/src/views/ai/assistant/composables/conversation-events.js`
  - 将后端历史消息转换为页面事件。
- `smart-web/src/views/ai/assistant/composables/conversation-events.test.js`
  - 会话切换加载历史消息的最小测试。
- `smart-web/src/views/ai/assistant/composables/use-chat-stream.js`
  - SSE 对话流。
- `smart-web/src/views/ai/assistant/composables/use-attachments.js`
  - 附件状态、上传完成 ID 和发送附件列表。
- `smart-web/src/components/ai/attachment-upload/index.vue`
  - 复制粘贴附件、OSS 上传、预览、进度和删除。
- `smart-web/src/api/ai/conversation-api.js`
  - 会话列表、详情、创建、改标题、删除接口。
- `smart-web/src/lib/axios.js`
  - 修复无 `Content-Type` 的 204 响应处理。

### 15.4 已完成的后端功能

涉及工程：`F:\project\gongcheng\agent` 和 `F:\project\gongcheng\smart-boot`

- 会话、消息、运行记录持久化。
- 会话详情查询及消息历史返回。
- 会话标题修改接口。
- 会话删除接口，并按租户、用户和会话 ID 校验数据范围。
- 删除会话时按顺序清理：`ai_citation`、`ai_run_step`、`ai_run`，再删除会话和消息，解决外键约束错误。
- SSE `/agent/chat/stream` 基础对话链路。
- 外部 OpenAI 兼容模型配置支持，当前开发配置曾使用：
  - Base URL：`https://api.iruidong.com/v1`
  - 模型：`glm-4.6`
  - API Key 由本地配置文件提供，不写入本文档。
- Agent 控制台增加模型调用和异常日志，便于排查模型请求失败。
- 附件上传、附件状态和附件随对话请求的基础改造已存在，但仍需继续验证“图片/文件是否真正随消息到达模型并参与回答”。

### 15.5 当前最近修复的问题

1. 左侧点击会话后右侧内容不变化。
   - 根因：原先只更新 `current`，没有加载详情并更新 `events`。
   - 处理：新增 `loadConversationEvents`，左侧切换和首次加载共用历史加载流程，并增加请求序列保护。

2. 删除聊天时数据库外键错误。
   - 根因：`ai_run`、`ai_run_step`、`ai_citation` 仍引用会话。
   - 处理：删除会话前先按租户和用户清理关联运行数据。

3. 删除成功后确认框不关闭。
   - 根因之一：删除接口返回 204，前端响应拦截器对缺失 `Content-Type` 调用了 `indexOf`。
   - 处理：`axios.js` 对空响应直接放行；确认框只在删除 Promise 成功后调用 `destroy()`。

### 15.6 当前待验证和待开发事项

- 重新编译并启动 `smart-agent`、`smart-boot`、`smart-web` 后验证会话删除完整流程。
- 验证删除最后一个会话、删除当前会话、删除有消息/运行记录的会话。
- 验证左侧连续快速切换不同会话时历史消息不会串会话。
- 验证附件上传成功后附件 ID 是否进入聊天请求，而不是只停留在前端预览状态。
- 验证图片和文字组合发送后，Agent 是否能收到附件信息并返回结果。
- 验证单独发送附件时后端是否保存用户消息、附件引用和模型请求上下文。
- 接入 `smart-boot` 业务查询工具，使智能体能够在权限范围内查询项目、合同、进度等业务数据。
- 完成业务工具权限校验、数据范围校验、工具调用审计和结果大小限制。
- 完成知识库页面和知识检索引用链路的联调。
- 不要因为当前页面聊天可演示，就宣称已经具备全部业务数据查询能力；业务工具仍需单独接入和验证。

### 15.7 新对话建议的第一步

新对话启动后，先确认以下信息，再继续编码：

1. 用户当前要继续处理的具体问题（优先处理删除流程或附件随消息发送）。
2. 用户是否已重新编译并启动三个服务。
3. 浏览器控制台和 Network 中对应请求的状态码、响应内容和后端日志。
4. 不重复实现本文 15.2 和 15.4 已完成的功能。

继续开发时仍遵守：只修改代码、不代编译；每完成一个独立任务暂停并等待用户确认；所有沟通和新增文档使用中文。
