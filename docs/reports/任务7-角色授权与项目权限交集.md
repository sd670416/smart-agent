# 任务7：角色授权与项目权限交集

## 完成内容

- 实现角色到知识空间授权的替换与实时读取。
- 多角色授权采用并集；无角色或已删除角色授权不会获得空间。
- 严格校验租户隔离，仅解析已发布知识空间。
- 项目空间必须同时满足角色授权和项目权限交集，租户空间不绕过租户校验。
- 授权管理要求 `ai:knowledge:manage` 权限。
- Boot 增加 `POST /internal/ai/permissions/projects/check`，只返回可信上下文中可见的请求项目 ID。

## 验证

Agent 编译通过；Boot 使用 `mvn -pl smart-ai -am` 编译通过。完整聚焦测试将在提交前执行。
