# 工程项目 AI 能力建设方案与开发进度

更新时间：2026-09-11

## 1. 文档目的

本文基于《副本工程项目（施工方）功能表260903_AI(1).xlsx》中的 AI 标记功能，结合当前 `smart-agent`、`smart-web` 和 `smart-boot` 的实现情况，整理 AI 能力建设方案、权限使用方式、数据路由规则、开发顺序和当前进度。

本文作为后续 AI 能力开发、接口确认和阶段验收的工作基线。功能表中黄色“加AI功能”列标记为勾选的条目，视为候选 AI 能力范围，具体上线仍需经过权限、数据接口和指标口径确认。

## 2. 总体设计

### 2.1 建设原则

- 用户侧只保留一个“工程管理智能助手”入口。
- `smart-boot` 继续作为业务事实、业务规则和最终权限判断的权威系统。
- `smart-agent` 不直接连接现有工程系统的生产业务表，不允许任意 SQL。
- 实时业务查询通过 `smart-boot` 受控接口完成。
- 制度、规范、合同和项目文件通过知识库检索完成。
- 需要写入业务系统的操作，先生成草稿，经过用户确认和权限复核后再执行。
- 所有回答、工具调用、知识引用和异常都应可审计。

### 2.2 系统调用关系

```text
smart-web
  -> HTTPS + SSE
smart-agent
  -> 身份上下文、问题路由、工具编排、知识检索、模型网关、审计
  -> smart-boot 内部 AI 业务接口
smart-boot
  -> 原有业务 Service / Mapper / MyBatis 数据权限拦截器
```

## 3. 查询数据路由设计

查询路径由问题类型、已注册能力和用户权限共同决定，不能由模型自由选择数据库或生成 SQL。

| 数据类型 | 执行方式 | 典型问题 |
|---|---|---|
| 实时项目、合同、进度、财务、库存数据 | `smart-boot` 业务接口 | 项目当前进度、合同金额、未收款金额 |
| 制度、规范、合同条款、项目文档 | 知识库检索 | 工期延误依据、材料验收规范 |
| 会话、运行记录、审计、知识元数据 | `smart-agent` 自有数据库 | 历史对话、工具调用记录 |
| 生产业务表 | 禁止直连 | 不允许智能体自由查询 |

同时涉及业务事实和文档依据的问题，需要分别调用业务工具和知识检索，再由模型进行对比分析。例如“项目当前进度是否符合合同约定”，业务工具提供实际进度，知识库提供合同工期条款。

工具注册时应声明数据来源和权限要求，例如：

```text
project.getOverview
来源：BUSINESS_API
执行：smart-boot
权限：原系统项目查询按钮权限

knowledge.search
来源：KNOWLEDGE_BASE
执行：Qdrant + MySQL
权限：knowledge:read
```

## 4. 权限设计

### 4.1 权限复用原则

不另建一套与原系统平行的角色权限体系。原系统的角色、菜单、按钮权限和数据权限继续作为最终依据，智能体只消费经过认证的权限上下文。

### 4.2 三层校验

#### 第一层：可信用户上下文

由 `smart-boot` 生成并签名，传递给 `smart-agent` 的上下文包括：

- 租户 ID；
- 用户 ID；
- 角色、部门和公司信息；
- 原系统权限编码；
- 菜单和按钮权限；
- 数据权限类型和范围；
- 可访问项目范围；
- TraceId 和过期时间。

前端提供的 `tenantId`、`userId`、权限 Header、项目范围等只能作为提示，不能覆盖可信上下文。

#### 第二层：智能体工具校验

`smart-agent` 根据可信上下文筛选可用工具，并在执行前校验：

- 用户是否具备对应原系统按钮权限；
- 项目是否在可信项目范围内；
- 租户和用户是否匹配；
- 输入字段是否包含未知字段；
- 调用次数、超时和结果大小是否符合限制。

#### 第三层：`smart-boot` 最终复核

业务接口最终仍进入原有业务 Service、Mapper 和 MyBatis SQL 拦截器，继续执行原系统的数据范围过滤。`smart-agent` 不应通过 JDBC 或自由 SQL 绕过这条链路。

### 4.3 数据权限边界

如果 AI 查询接口复用原有业务 Service 和 Mapper，MyBatis SQL 拦截器可以继续生效。如果新增接口绕过原业务链路直接查询业务表，则原数据权限可能失效，因此禁止这种实现方式。

## 5. AI 能力范围

### 5.1 流程、待办和消息

功能表中已标记的能力包括首页待办、消息中心和手机端我的审批。

可实现能力：

- 查询待办和未读消息；
- 按紧急程度和截止时间排序；
- 总结审批事项；
- 提示质保金、保证金等预警；
- 查看流程节点、办理人和审批意见。

第一阶段只读查询，不直接办理、转办、退回或终止流程。

### 5.2 项目、合同和档案查询

功能表中已标记的项目报备、投标、证件、合同和各类档案，是最适合优先建设的范围。

建议工具：

```text
project.getOverview
project.getContracts
project.getProgress
project.getArchiveSummary
tender.getOverview
contract.getSummary
certificate.getExpiring
supplier.getProfile
```

第一版优先提供项目基础信息、合同汇总、项目状态、进度和档案摘要。

### 5.3 经营分析和看板

包括经营状况、预算执行、应收账款、供方账款、投标、库存和进度分析。

这类能力需要多个业务接口组合，并在开发前确认：

- 时间范围；
- 金额口径；
- 汇总维度；
- 项目和公司范围；
- 已收、已付、已结算等指标定义。

### 5.4 现场和过程数据

包括进度计划、进度反馈、施工日志、材料入库、库存盘点和项目看板。

第一阶段可先处理结构化文字和业务数据。施工日志图片、安全检查图片等视觉能力，需在视觉模型或图片解析方案确定后接入。

### 5.5 写操作辅助

投标报名、中标确认、付款申请、项目报销、材料入库等功能涉及业务写入，采用以下阶段：

```text
查询和解释
  -> 生成业务草稿
  -> 展示待提交字段
  -> 用户确认
  -> 权限复核
  -> 调用原系统写接口
  -> 审计记录
```

一期不提供无需确认的自主写操作。

## 6. 开发任务安排

### 阶段一：业务查询基础

1. 确认项目概况接口、Service 和原系统按钮权限编码。
2. 实现 `AgentTool`、`ToolContext` 和 `ToolExecutor` 的统一调用约束。
3. 实现 `project.getOverview`。
4. 增加租户、用户、项目范围、按钮权限、输入字段、超时和 64KB 结果限制。
5. 增加工具调用审计和稳定错误码。

### 阶段二：项目相关查询

1. 项目合同汇总。
2. 项目进度和进度偏差。
3. 项目档案摘要。
4. 投标档案和开标结果。
5. 项目风险和预警摘要。

### 阶段三：待办、消息和档案

1. 待办和审批事项查询。
2. 消息中心和预警查询。
3. 证件有效期查询。
4. 供应商、甲方单位和公司档案查询。

### 阶段四：经营分析和看板

1. 预算执行分析。
2. 应收和供方账款分析。
3. 投标分析。
4. 库存分析。
5. 进度甘特图和项目看板摘要。

### 阶段五：文件解析和知识库

1. XLSX、PDF、Word、TXT 等附件解析。
2. 解析任务记录、状态下拉和重试。
3. 知识空间、文档版本和权限过滤。
4. 文档引用和回答依据。
5. 施工日志和现场附件的结构化分析。

### 阶段六：受控业务操作

1. 生成付款、报销、投标等业务草稿。
2. 展示字段差异和风险提示。
3. 用户确认后复核权限。
4. 调用原系统写接口并完整审计。

## 7. 当前开发进度

### 7.1 已完成

- `smart-agent` 基础工程、会话、消息和运行记录。
- 可信工程用户上下文、签名校验和 TraceId。
- SSE 对话链路和模型网关基础能力。
- 前端智能助手页面、会话切换、历史消息、标题修改和删除。
- 图片和普通文件的上传、消息展示和基础持久化。
- SSE 错误提示持久化。
- 首次发送文字自动更新会话标题。
- 消息发送后自动滚动到底部。

### 7.2 当前已验证

- 前端 `/ai/chat/stream` 请求能够携带 `attachmentIds`。
- 图片附件状态和图片模型不支持错误已能区分。
- 图片和文件可以在当前聊天窗口显示。
- 会话删除和会话切换问题已完成修复。

### 7.3 当前限制

- 当前 `glm-4.6` 接口拒绝图片多模态请求，需要视觉模型或其他图片处理方案。
- XLSX 等普通文件目前可以上传、显示和记录，但尚未解析内容并加入模型上下文。
- 解析任务记录下拉、失败原因和重试能力尚未实现。
- 合同、进度等后续业务查询工具尚未接入；项目通用语义查询已接入 `smart-boot`。
- 已完成项目通用语义查询 `project.query`：支持业务字段筛选、排序、分组、聚合和分页；模型不得提交 SQL、数据库列名或权限范围，可信项目范围由 Agent 注入并由 smart-boot 最终复核。
- 已增加项目查询专用错误码和中文提示，空可信范围直接返回空结果；查询列表和统计结果要求使用中文表格及口径说明。
- Qdrant 知识库完整联调仍依赖局域网环境。

## 8. 首个开发任务：`project.getOverview`

### 8.1 目标

在用户权限范围内查询一个项目的基础概况，作为第一个完整业务工具样板。

### 8.2 输入

```json
{
  "projectId": "项目 ID"
}
```

只允许 `projectId`，拒绝未知字段和伪造租户、用户、权限字段。

### 8.3 输出建议

```text
项目名称
项目编号
项目状态
项目类型
建设单位
施工单位
项目负责人
计划开工日期
计划竣工日期
合同金额
累计完成产值
累计回款
当前进度
风险状态
```

最终字段以 `smart-boot` 现有业务接口为准。

### 8.4 完成标准

- 有项目查询权限才能调用；
- 项目不在用户可信范围时拒绝，且不调用业务客户端；
- `smart-boot` 最终复用原有数据权限链路；
- 未知工具、未知字段、超时和超过 64KB 的结果有稳定错误；
- 工具调用有审计记录；
- 专项测试、全部非 Docker 测试和独立代码审查通过。

## 9. 待确认事项

后续逐项确认，不一次性假设：

1. `smart-boot` 是否已有项目概况接口或 Service。
2. 该查询对应的原系统菜单、按钮权限编码。
3. AI 内部接口是否复用原有业务 Service、Mapper 和 MyBatis SQL 拦截器。
4. 项目数据范围在可信用户上下文中的具体字段。
5. 项目概况第一版字段清单。
6. 合同、进度、档案等模块的现有查询入口。
7. 经营分析指标的统计口径和时间范围。
8. 真实视觉模型、Embedding 模型和模型网关配置。

## 10. 执行规则

- 每个独立任务先写失败测试，再实现最小代码。
- 完成专项测试、全量非 Docker 测试和代码审查后再进入下一任务。
- 每个任务保持小范围提交，可独立回退。
- 不提交 `.env`、密码、签名密钥或 API Key。
- 不启动本地 Docker；Qdrant 依赖按局域网环境执行。
- 用户自行编译和启动，开发过程中只修改代码和文档并汇报变更。
- 所有权限判断来自可信上下文，并由 `smart-boot` 最终复核。
